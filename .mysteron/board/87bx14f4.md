---
title: Memory
state: bin
priority: medium
companionId: 82be1826-9999-453f-bbfc-999f8dc4a2dc
assignee: Zoot the Swift
labels: []
created: '2026-06-26T21:09:08.847Z'
updated: '2026-06-27T10:53:56.599Z'
---

Currently nothing uses memory. Could we start adding things to it - this should be seen as a shared context, if an agent needs to read a file to find out if it's responsible for something it should report when it finds out it is. Maybe make it mimic the src file structure, make sure agents commit to it as they do their regular work. Remove the 'one fact per file'

---

**Done (Zoot the Swift):**
- `src/core/memory.ts`: dropped the one-fact-per-file model. Memory names may now be nested (e.g. `core/board`) so memory mirrors the `src/` tree — `write_memory` creates parent dirs and `list_memories` recurses. Path traversal outside the memory dir is still rejected. Docstring updated; a file may hold many related facts.
- `src/mcp/server.ts`: memory tool descriptions now frame memory as shared context, nudge agents to read it before working an area and to record what they learn (e.g. discovering ownership) as they go.
- Etiquette: added a "Keep memory current" rule (written to shared docs via MCP).
- Seeded an initial `memory-subsystem` entry documenting the model.
- New test in `test/core.test.ts` covers nested names, recursive listing, multi-fact files and traversal rejection. Full suite: 93 pass, typecheck clean.

Note: the live MCP server runs the host's pre-merge build, so nested memory names (and the `core/memory` home for the seeded entry) only become writable once this commit is merged and the server restarts.
